size = dict(
    x=1000,
    y=1000
)

size_tile = dict(
    x=105,
    y=105
)

scale = dict(
    v_line=size["x"] / 8,
    v_line_size=1,
    h_line=size["y"] * 0.95,
    h_line_size=5
)

color = dict(
    line=(0, 0, 0),
    tile=(40, 40, 40)
)
